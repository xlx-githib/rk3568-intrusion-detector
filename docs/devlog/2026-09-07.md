# 2026-09-07  目标：D4 移植 C++ 推理引擎 + D5/D6 ROI 状态机 → **M1 视频文件闭环雏形达成**

## 今日完成
- [√] 编写 `src/infer/rknn_engine.hpp/.cpp`：RKNN 六连封装 + YOLOv5/v7 三尺度解码 + NMS + letterbox + **person/car 白名单**，输出 `DetObject`(含底部中心，供 ROI 用)
- [√] `src/main.cpp` 最小检测入口：读 RGB 裸图 → 推理 → 打印/画框(PPM)，**零 OpenCV 依赖**
- [√] CMake 支持 RKNN 交叉编译（`-DRKNN_INCLUDE_DIR / -DRKNN_LIB_DIR`）
- [√] **交叉编译 + 板上真机验证通过**：`bus.rgb` 检出 **3 个 person**（conf 0.91/0.83/0.78），与 python 基线一致（低分 0.27 被 conf 过滤属正常差异）

## 遇到的问题 / 怎么解决的（都是交叉编译的典型坑）
- **问题：编错架构** —— 默认 `/usr/bin/c++` 是 x86，需换 **aarch64 交叉编译器**（用 SDK buildroot 的 `aarch64-buildroot-linux-gnu-g++`）。
- **问题：`ld: 找不到 -lrknnrt`** —— rknpu2 的库不在 `librknn_api/lib`，而在 **`librknn_api/aarch64/`** 子目录（Linux 下分 armhf/aarch64）。
- **问题：`rknn run` 报 `failed to submit! OutputOperator ... size_with_stride`** —— 对量化模型设 `want_float=0`(取 int8) 会触发 NPU 跑 OutputOperator，此模型/版本不支持。
  解决：**统一 `want_float=1`(取 float) + fp 解码**，与板上 python(rknnlite) 验证路径一致 → 成功。
  备注：后续要 int8 高性能可改用 `rknn_create_memory(size_with_stride)` 零拷贝。
- **注意：runtime 版本匹配** —— 头/库用 `rknpu2-1.5.0`（与板上 `librknnrt 1.5.0` 一致），不能用 SDK 老版本。

## D5/D6（同日补记）：ROI 状态机 + M1 闭环雏形
- [√] 实现 `src/business/roi_monitor.hpp/.cpp`：ROI 事件状态机（IDLE→INSIDE→ALARMED，
      帧级判定：白名单目标"框底部中心"落 ROI 内即视为进入；停留超时触发告警；离开发 LEAVE/RESOLVE）
- [√] `main.cpp` 增加 **video 帧序列模式**：逐帧读 `.rgb` → 检测 → ROI 判定 → 事件打印 + **告警帧截图(shots/*.ppm)**
- [√] 板上抽帧工具 `tools/board/video_to_frames.py`（mp4 → 帧序列 + meta）
- [√] **板上闭环验证通过**：444 帧 @640×289 / 10fps，实测 `INTRUDE(帧0) → ALARM(帧30, stay=3000ms) + 告警截图`
  → **M1「视频文件→检测→ROI→告警截图」闭环雏形达成**
- 踩坑：CMake `GLOB_RECURSE` 不捕获后加的 .cpp → 改用**显式源文件列表**（新增文件需手动登记）

## 里程碑进度
- ✅ D1~D3（读 demo + 真机验证链路）+ D4（自己的 C++ 推理封装真机出框）+ **D5/D6 → M1 闭环雏形达成**
- ▶ 下一步：M2（真 MIPI 摄像头 V4L2 + 多线程流水线 + socket 上报 PC）

## 用时 / 数据
- 用时：D4 约 5h + D5/D6 约 5h（含交叉编译与 CMake 踩坑）
- 真机数据：yolov7-tiny 640 输入单图推理毫秒级；闭环 444 帧全程约数十秒（含打印开销）；
  帧级耗时与 CPU/NPU 占用待 M2 用 `tools/bench.sh` 补测
- 成果：视频中人物进入 ROI → 停留 3s → 告警帧截图（shots/）
