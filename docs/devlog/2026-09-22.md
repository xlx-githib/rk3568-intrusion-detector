# 2026-09-22  目标：项目"企业级"升级启动 —— 迭代1：运行时控制台 + 参数热更新

> 说明：09-09~09-21 期间主线在求职准备与简历；今日回到项目做能力升级。
> 方向确认（板上环境已核实）：**板端 Qt 5.15.2、librockchip_mpp、GStreamer mpp 硬编码插件、
> /dev/fb0 + /dev/dri 显示** 全部就绪，可支撑"板端 Qt + H.264/H.265 推流"两阶段升级。

## 今日完成
- [√] 环境核实（板上/SDK 两侧）：
  - 板：`libQt5Core.so.5.15.2`、`librockchip_mpp.so`、`gst-inspect` 可见 `mpph264enc/mpph265enc/mppvideodec/mppjpegenc`、`/dev/fb0` + `/dev/dri/card0`
  - SDK：`external/mpp`（源码+doc）、buildroot `host/bin/qmake`（Qt 5.15 交叉工具链）
- [√] **新增运行时控制台**（`src/common/console.hpp/.cpp`）：
  - `RtParams`（原子参数：ROI/stay/去抖/conf/nms/日志级别/上报开关 + version 号）
  - `EventLog`（最近 200 条事件环形记录）
  - `Console`（独立线程 `poll(stdin)` + 命令解析 + 配置文件 save/reload）
  - 命令：`help/status/roi/stay/leave/conf/nms/log/report/events/save/reload/quit`
- [√] **参数热更新接线**：
  - `RoiMonitor` 增 `setRoi/setStaySec/setLeaveConfirm`（不重置状态）
  - `RknnEngine` 增 `setThresh`（阈值改原子变量，解码/NMS 运行时读取）
  - `main.cpp`(pipe)：控制台线程 + Business 线程按 `version` 变化应用参数 + 事件记入 EventLog
    + `report on|off` 即时生效 + `quit` 优雅收尾（采集→逐级 flush→断开上报）
- [√] 文档与配套：`docs/console.md`（命令手册/原理/扩展点）、`tools/board/h264_test.sh`（MPP 硬编码验证，迭代2 铺垫）

## 遇到的问题 / 怎么解决的
- **参数热更新怎样不破坏流水线**：不用锁，改为"原子参数 + 版本号"——控制台只写、Business 线程比对
  `version` 后统一应用（`setRoi/setStaySec/setLeaveConfirm/setThresh`），控制路径与数据路径解耦。
- **控制台不能阻塞主流程**：独立线程 + `poll(stdin, 200ms)` 超时轮询，兼顾"可交互"与"可退出"。
- **配置回写怕破坏注释**：`save` 只替换自己管理的 9 个键，其它行原样保留；`reload` 只认这些键。

## 里程碑进度
- ✅ M1 视频闭环 / M2 实时流水线+去抖+跨机 socket（09-08）
- ▶ **迭代1（本日）**：运行时控制台 + 参数热更新 —— 代码完成，待板上编译验证
- ▶ 迭代2：GStreamer/MPP **H.264 硬编码推流** → PC 真视频监控（可选 H.265 码率/延迟对比）
- ▶ 迭代3：板端 **Qt(EGLFS/DRM)** 界面（视频区 + ROI + 事件列表 + 参数设置页）
- 横切：OpenCV 仅用于 PC 端（录像/调试）；README 冻结与 Release v0.1 待做

## 用时 / 数据
- 用时：约 2 小时（含环境核实与方案确认）
- 使用说明见 `docs/console.md`；命令示例：运行中 `roi 300 200 600 400`、`conf 0.4`、`save`
